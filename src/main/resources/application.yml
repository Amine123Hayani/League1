spring:
  application:
    name: Matawan

  datasource:
    url: jdbc:h2:mem:footballdb
    driver-class-name: org.h2.Driver
    username: admin
    password: amine2000?

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
